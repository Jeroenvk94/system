build_settings:
    ignore:
        - "vendor"
        - "tests"

setup:
    composer:
        action: "install"

test:
    php_unit:
        config: 
            - "phpunit.xml"
        coverage: "coverage"
        args: "--stderr"
    php_mess_detector:
        allowed_warnings: 6
    php_code_sniffer:
        standard: "PSR2"
        allowed_warnings: 1
    php_cpd:
        allow_failures: true