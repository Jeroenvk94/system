build_settings:
    ignore:
        - "vendor"
        - "tests"
        - "reports"

setup:
    composer:
        action: "install"

test:
    php_unit:
        config: 
            - "phpunit.xml"
        coverage: "coverage"
        args: "--stderr"
    php_mess_detector:
        allowed_warnings: true
    php_code_sniffer:
        standard: "PSR2"
        allowed_errors: true
        allowed_warnings: true
    php_cpd:
        allow_failures: true